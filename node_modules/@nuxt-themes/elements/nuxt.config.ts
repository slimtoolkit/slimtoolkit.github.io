import { createResolver } from '@nuxt/kit'

const { resolve: resolveThemeDir } = createResolver(import.meta.url)

// https://v3.nuxtjs.org/api/configuration/nuxt.config
export default defineNuxtConfig({
  modules: ['@nuxt-themes/tokens'],

  components: [
    { path: resolveThemeDir('components/globals'), global: true, prefix: '' },
    { path: resolveThemeDir('components/icons'), global: true, prefix: '' },
    { path: resolveThemeDir('components/landing'), global: true, prefix: '' },
    { path: resolveThemeDir('components/volta'), global: true, prefix: '' }
  ],
})
